---
description: Show missionctl lifetime token-savings stats
---
Show missionctl stats — total tokens saved, sessions, average compression ratio.

Read the lifetime history log at `~/.config/missionctl/.missionctl-history.jsonl`
(or wherever the missionctl-stats script writes it). Output: total saved,
sessions counted, avg ratio. One short table.
