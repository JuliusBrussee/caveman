{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/JuliusBrussee/caveman/contracts-v2.0.0/packages/shared/contracts/schemas/middleware-receipt-response.schema.json",
  "title": "Caveman middleware receipts response",
  "description": "receipts 200 response. Closed on purpose: describes exactly what a protocol 1.1 server emits, and the conformance kit holds servers to it. Clients MUST NOT validate responses against this schema; they ignore unknown fields (docs/technical/middleware-protocol.md §4, §16).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "status",
    "basis",
    "verified_saved_usd"
  ],
  "properties": {
    "schema_version": {
      "const": 1
    },
    "status": {
      "const": "recorded"
    },
    "basis": {
      "const": "client_observed"
    },
    "verified_saved_usd": {
      "const": 0
    }
  }
}
