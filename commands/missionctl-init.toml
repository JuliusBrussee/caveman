description = "Drop the always-on missionctl activation rule into the current repo for every IDE agent"
prompt = "Run `node src/tools/missionctl-init.js {{args}}` in the current repo and report the result. Use --dry-run first if the user did not pass --force, so we never silently overwrite an existing rule file."
